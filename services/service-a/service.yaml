apiVersion: v1
kind: Service
metadata:
  name: service-a
spec:
  selector:
    app: service-a
  ports:
  - name: http
    port: 8001
    targetPort: http
  - name: metrics
    port: 8000
    targetPort: metrics